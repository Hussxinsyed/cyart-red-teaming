Practical Observation:

Wireshark was used to observe repeated HTTPS/TLS communication patterns.

Although HTTPS encrypts content, metadata such as timing, source IPs, destination IPs, and packet sizes remain visible for analysis.

Repeated outbound encrypted communication patterns may resemble beaconing behavior.
